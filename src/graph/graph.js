/** 
 * @class
 * @param {Object} config An object containing graph configutations (container, etc..)
 */
hGraph.Graph = function( config ) {
  
  var container = config.container || document.createElement('div');
  
  /** initializes the graph instance */
  this.Initialize = function( ) {
    
  };
  
  /** 
   * @private
   */
  function Draw( ){
    
  };
  
  /** 
   * @private
   */
  function Update( ) {
    
  };
  
};