{
  "name": "demo",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://goinvo.github.io/hgraph-react",
  "dependencies": {
    "react": "^16.3.2",
    "react-dom": "^16.3.3"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "react-scripts": "3.4.1"
  },
  "scripts": {
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
